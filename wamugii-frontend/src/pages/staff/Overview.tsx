import { PageStub } from '@/components/common/PageStub'

export function StaffOverview() {
  return (
    <PageStub
      title="Staff Overview"
      description="Built from /api/v1/projects and /api/v1/quote-requests lists in Stage 9 (no staff stats endpoint exists)."
    />
  )
}
